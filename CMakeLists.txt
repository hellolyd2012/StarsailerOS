# 创建于2024.11.1 
# StarsailerOS CmakeLists文件
# 执行Cmake -B Build编译

cmake_minimum_required(VERSION 3.25)

project(StarsailerOS)

enable_language(C)
enable_language(CXX)
enable_language(ASM)

include_directories(Includes)

set(StarsailerOSVersion Aphla 0.1)
